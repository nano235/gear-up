import React from 'react'
import styles from './AcceptDecline.module.scss'

interface Props {
    handleNext: () => void
}
const AcceptDecline = ({ handleNext }: Props) => {
    return (
        <div>AcceptDecline</div>
    )
}

export default AcceptDecline